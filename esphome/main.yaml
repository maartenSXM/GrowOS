#  yaml style comments are allowed
// C-style comments are allowed
/* traditional C-ctyle comments are too */

#include "config.h"
#include "pins.h"
#include "env.yaml"
#include "globals.yaml"
#include "numbers.yaml"
#include "debug.yaml"
#include "sensor.yaml"
#include "network.yaml"

#if _CPU == 0
# if GB_CONFIG_DISPLAY
#  include "display.yaml"
#  if GB_CONFIG_DISPLAY_LVGL
#   include "lvglMain.yaml"
#  endif // GB_CONFIG_DISPLAY_LVGL
# endif // GB_CONFIG_DISPLAY
# include "app0.yaml"
#else // _CPU == 1
# include "app1.yaml"
#endif // _CPU == 1
